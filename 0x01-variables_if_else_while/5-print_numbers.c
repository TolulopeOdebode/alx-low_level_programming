#include <stdio.h>
/**
 * main - All single digits in Base ten
 *
 * Return: Always 0 (Successful)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
